services:
  db:
    image: postgres:15
    container_name: pg_restaurant
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - 5444:5432
    env_file:
      - .env