spring:
  application:
    name: restaurant
  datasource:
    url: jdbc:postgresql://localhost:5444/restaurant
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration